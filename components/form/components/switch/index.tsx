import { Switch } from '@/components/ui/switch';
import type { SwitchProps } from '@radix-ui/react-switch';

export default function Component(props: SwitchProps) {
    return (
        <Switch
            {...props}
        />
    )
}
